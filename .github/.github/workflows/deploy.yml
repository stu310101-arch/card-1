name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]  # 監聽 main 分支的變動

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v3
        with:
          node-version: '18' # 指定 Node.js 版本，避免版本不相容導致失敗

      - name: Install and Build 🔧
        run: |
          npm install
          CI=false npm run build  # 關鍵修改：忽略警告，強制打包

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: build # React 打包後產生的資料夾名稱
