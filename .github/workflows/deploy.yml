name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]  # 如果你的主分支叫 master，請把這裡改成 master

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: build # React 打包後產生的資料夾名稱
